{
  "name": "coup-game",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "push": "git add . && git commit -m 'update' && git push",
    "dev": "concurrently -n server,client -c cyan,yellow \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
